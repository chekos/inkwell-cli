NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only. Do not expose this to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# Modal worker dispatch. Keep disabled for local UI work without a worker.
INKWELL_WORKER_DISPATCH_ENABLED=false
INKWELL_WORKER_ENDPOINT=
INKWELL_WORKER_TOKEN=
